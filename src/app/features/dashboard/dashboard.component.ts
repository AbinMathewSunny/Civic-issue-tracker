import { Component, computed, inject } from '@angular/core';
import { MatCardModule } from '@angular/material/card';
import { IssueService } from '../../core/services/issue.service';
@Component({ selector: 'app-dashboard', imports: [MatCardModule], template: '<p class="eyebrow">Community overview</p><h1>Make your neighbourhood better</h1><p>Track reported civic issues and their progress in one place.</p><section><mat-card><span>Total issues</span><strong>{{ total() }}</strong></mat-card><mat-card><span>Open</span><strong>{{ open() }}</strong></mat-card><mat-card><span>Resolved</span><strong>{{ resolved() }}</strong></mat-card></section>', styles: ['h1{margin:0;color:#102a43;font-size:2.3rem}.eyebrow{color:#0b7285;font-weight:700;text-transform:uppercase;font-size:.78rem;letter-spacing:.08em}p,span{color:#627d98}section{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem;margin-top:2rem}mat-card{padding:1.4rem}strong{display:block;margin-top:.4rem;font-size:2rem;color:#102a43}@media(max-width:600px){section{grid-template-columns:1fr}}'] }) export class DashboardComponent { private service=inject(IssueService); protected issues=this.service.issues; protected total=computed(()=>this.issues().length); protected open=computed(()=>this.issues().filter(i=>i.status==='Open').length); protected resolved=computed(()=>this.issues().filter(i=>i.status==='Resolved').length); }
