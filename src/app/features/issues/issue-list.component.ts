import { Component, inject } from '@angular/core';
import { MatCardModule } from '@angular/material/card';
import { MatChipsModule } from '@angular/material/chips';
import { IssueService } from '../../core/services/issue.service';
@Component({ selector: 'app-issue-list', imports: [MatCardModule, MatChipsModule], template: '<p class="eyebrow">Civic reports</p><h1>All issues</h1><p>Sample reports for Phase 1. Phase 2 adds issue creation and local storage.</p><section>@for (issue of issues(); track issue.id) { <mat-card><mat-card-header><mat-card-title>{{ issue.title }}</mat-card-title><mat-card-subtitle>{{ issue.location }}</mat-card-subtitle></mat-card-header><mat-card-content><p>{{ issue.description }}</p><mat-chip-set><mat-chip>{{ issue.status }}</mat-chip><mat-chip>{{ issue.priority }} priority</mat-chip></mat-chip-set><small>Reported by {{ issue.reportedBy }} · {{ issue.createdAt }}</small></mat-card-content></mat-card> }</section>', styles: ['h1{margin:0;color:#102a43;font-size:2rem}.eyebrow{color:#0b7285;font-weight:700;text-transform:uppercase;font-size:.78rem;letter-spacing:.08em}p,small{color:#627d98}section{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:1rem;margin-top:2rem}mat-card{min-height:230px}mat-card-content{padding-top:1rem}mat-chip-set{margin:1rem 0}'] }) export class IssueListComponent { protected issues=inject(IssueService).issues; }
